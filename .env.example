# Copia este archivo a .env y ajusta los valores segun tu maquina.
# cp .env.example .env

# Carpeta con los 5 CSV originales de CICIDS2017 (Monday...Friday-WorkingHours.csv)
# DATA_DIR=./data/raw

# Carpeta donde se guardan los .parquet, el scaler y los reportes del pipeline
# OUTPUT_DIR=./data/processed

# Semilla para reproducibilidad (splits, inicializacion del modelo, etc.)
# RANDOM_SEED=42
